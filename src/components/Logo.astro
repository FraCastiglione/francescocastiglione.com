---
import Wordmark from './Wordmark.astro';

interface Props {
  compact?: boolean;
  hero?: boolean;
  light?: boolean;
}

const { compact = false, hero = false, light = false } = Astro.props;
---

<span class:list={['logo', { 'logo--compact': compact, 'logo--light': light }]}>
  <Wordmark compact={compact} hero={hero} light={light} />
</span>

<style>
  .logo {
    display: inline-flex;
    align-items: center;
  }
</style>
